/**
 * All numeric thresholds for the emotion color system.
 * Change here — never hardcode in component files.
 */
export const EMOTION_COLOR_CONFIG = {
  TINT_OPACITY_MIN:  0.02,   // whisper — barely perceptible warmth
  TINT_OPACITY_MAX:  0.05,   // ceiling keeps text fully readable
  SARCASM_THRESHOLD: 0.50,
};
